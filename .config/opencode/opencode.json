{
  "$schema": "https://opencode.ai/config.json",
  "default_agent": "plan",
  "agent": {
    "build": {
      "steps": 1 // always just do one thing at a time, lets see
    }
  },
  "plugin": [],
  "mcp": {
    "Google Chrome MCP": {
      "type": "local",
      "command": ["npx", "chrome-devtools-mcp@latest"]
    }
  }
}
